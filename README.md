# 🤖 BOSS直聘自动投递助手

> Tampermonkey 用户脚本：在 BOSS直聘职位列表页一键自动投递，支持 **AI 定制打招呼语**、自定义模板/技能、自动翻页、跨页面会话恢复。

[![Version](https://img.shields.io/badge/version-4.6-brightgreen)]()
[![License](https://img.shields.io/badge/license-MIT-blue)]()
[![Platform](https://img.shields.io/badge/platform-Tampermonkey-orange)]()

---

## 📌 项目简介

本脚本在浏览器端（Tampermonkey）运行，模拟人工求职流程：

1. 打开 BOSS直聘搜索页（如搜索"AI Agent"）
2. 脚本自动逐条点击职位卡片 → 读取岗位信息
3. 生成打招呼语（**AI 生成 / 自定义 / 模板** 三种模式）
4. 自动点击「立即沟通」→ 跳转聊天页 → 自动发送
5. 发送成功后自动返回列表页，继续下一个职位
6. 达到每日上限或手动停止即结束

全程无需打开开发者工具、无需 Python 环境，**普通浏览器 + Tampermonkey 即可运行**。

---

## ✨ 功能特性

| 功能 | 说明 |
|------|------|
| 🎯 一键自动投递 | 点击「开始」自动循环投递，支持自动翻页 |
| 🤖 AI 生成招呼语 | 接入硅基流动（SiliconFlow）API，根据岗位 JD 定制打招呼语 |
| 📝 自定义招呼语 | 可直接填写固定招呼语，支持 `{title}` `{company}` 变量 |
| 🧩 自定义模板 | 多条模板可编辑（`---` 分隔），AI 按模板风格生成，也可随机选用 |
| 🛡️ 技能约束 | 填写"我的技能"后，AI **只允许使用列表内的技能，严禁编造** |
| ⏱️ 可调参数 | 每日投递上限（1~200）、每次间隔秒数（5~600）均可在面板配置 |
| 💾 配置持久化 | API Key / 模型 / 模板 / 技能 / 参数全部自动保存，配置一次永久生效 |
| 🔁 跨页面恢复 | 跳转聊天页后自动发送并返回列表页继续，会话 15 分钟内可自动恢复 |
| 🚫 去重防骚扰 | 已投递 / 发送失败的职位自动记录，当天不重复投递 |
| 🛑 安全停止 | 任意时刻可点「停止」，最多 1~2 秒内完全停下（不会偷偷续跑） |

---

## 📦 环境要求

| 依赖 | 说明 |
|------|------|
| 浏览器 | Chrome / Edge 等 Chromium 内核浏览器 |
| 扩展 | [Tampermonkey（油猴）](https://www.tampermonkey.net/) |
| 账号 | BOSS直聘已登录账号 |
| （可选） | 硅基流动 [API Key](https://cloud.siliconflow.cn/account/ak)（不填则用模板招呼语） |

### 🔑 硅基流动 API Key 获取

1. 点击下方链接注册/登录硅基流动（新用户填写邀请码可领 **16 元全平台奖励券**）：
   👉 **邀请链接：** https://cloud.siliconflow.cn/i/ADlfbLEX
   👉 **邀请码：** `ADlfbLEX`
2. 登录后进入 [API 密钥管理页](https://cloud.siliconflow.cn/account/ak)，点击「新建 API 密钥」
3. 复制生成的密钥，粘贴到脚本面板「AI 配置」页的 **API Key** 输入框即可

> 💡 新用户填写邀请码 `ADlfbLEX` 后可领取 **16 元额度的全平台可用奖励券**，足够免费体验 AI 定制招呼语功能。

---

## 🚀 快速开始

### 1. 安装 Tampermonkey

- Chrome 应用商店搜索 **Tampermonkey** 并安装
- 安装后浏览器右上角会出现 🐒 图标

### 2. 导入脚本

方式 A（推荐，获取更新）：

1. 打开 Tampermonkey 管理面板 → 「实用工具」标签
2. 「从 URL 导入」输入：`https://raw.githubusercontent.com/sanyuewujun/BOSSAotoApply/main/boss-auto-apply.user.js`
3. 点击「安装」→ 确认

方式 B（本地导入）：

1. 下载本仓库的 `boss-auto-apply.user.js`
2. Tampermonkey 管理面板 → 「实用工具」→ 选择文件 → 导入 → 安装

### 3. 验证安装

打开 BOSS直聘任意职位搜索页（如 `https://www.zhipin.com/web/geek/jobs?query=AI`），页面右下角出现 **「🤖 AI投递」悬浮面板** 即安装成功。

---

## ⚙️ 配置说明

点击面板右上角 **「−」** 可折叠/展开；面板内有两个标签页。

### ⚙️ 投递配置页

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| 今日计数 | 显示已投递数量 / 每日上限 | — |
| AI 开关 | 勾选后使用 AI 生成招呼语；取消则用自定义或模板 | ✅ 开启 |
| 每日上限 | 当天最多投递次数（1~200） | 30 |
| 间隔(s) | 每次投递之间的等待秒数（5~600） | 30 |
| 自定义招呼语 | 固定招呼语，留空则走 AI/模板；支持 `{title}` `{company}` | 空 |

> 💡 自定义招呼语优先级最高：**自定义 > AI 生成 > 第一条模板**。

### 🤖 AI 配置页

| 配置项 | 说明 |
|--------|------|
| API Key | 硅基流动密钥，**仅需配置一次**，自动保存；留空则不调用 AI，直接使用模板 |
| 模型选择 | 点击 **🔄 刷新列表** 从硅基流动 API **实时获取**全部文本模型（不写死）；支持关键词过滤，如输入 `Qwen` / `DeepSeek` |
| 招呼语模板 | 多条模板用 `---` 分隔，支持 `{title}` `{company}` 变量；清空恢复默认 |
| 我的技能 | 逗号分隔填写真实技能，**AI 只允许使用列表中的技能，严禁编造** |

**AI 生成时的硬性约束（写入 System Prompt）：**
- 只能使用「我的技能」中真实掌握的技能，严禁编造、夸大
- 写作风格参考所选模板
- 第一人称，120 字以内，直接输出

---

## 🔄 工作原理

```
┌─────────────────────────────────────────────────────────┐
│  职位列表页 (/web/geek/jobs)                             │
│                                                         │
│  点击职位卡片 → 读取 岗位/公司/JD → 生成招呼语            │
│        │                                                │
│        ▼ 点击「立即沟通」前保存会话到 GM 存储             │
│                                                         │
│  跳转到聊天页 (/web/geek/chat)                           │
│  找到输入框 → 填入招呼语 → 点击发送 → 校验成功            │
│  记录已投递 → 跳回列表页（全量刷新）                     │
│        │                                                │
│        ▼ 会话仍在有效期（15 分钟）内                      │
│  列表页自动恢复循环 → 跳过已投/失败 → 下一张卡片          │
└─────────────────────────────────────────────────────────┘
```

**关键设计：**

- **跨页面会话**：循环状态、当前职位、招呼语保存在 GM 存储中，页面跳转不丢失
- **防死循环**：跳回列表页使用全量刷新（`location.href`），而非 `history.back()`，避免浏览器 bfcache 复活旧上下文导致无限循环
- **发送校验**：发送后检查输入框是否被清空，未清空自动重试（最多 3 次）
- **停止即停**：每个等待点（点击卡片、AI 等待、发送重试）后都会检查停止标志，停止后不会重新写入运行状态
- **去重机制**：`bap_applied_日期` 记录已投职位，`bap_skip_日期` 记录发送失败职位

---

## 💰 费用估算（硅基流动）

每次投递约消耗 **500~700 tokens**（输入：模板+岗位+JD；输出：招呼语）。以下为常见模型参考价（**模型完整列表可在面板中点击「🔄 刷新列表」实时获取**）：

| 模型 | 单价 | 每次成本 | 约可投递次数/元 |
|------|------|----------|:---:|
| THUDM/GLM-4-9B-0414 | 免费（限速） | ¥0 | **无限** |
| Qwen/Qwen2.5-7B-Instruct | ¥0.35/M | ≈ ¥0.00025 | **约 4000 次** |
| Qwen/Qwen2.5-14B-Instruct | ¥1.3/M | ≈ ¥0.0009 | 约 1100 次 |
| Qwen/Qwen2.5-72B-Instruct | ¥4.13/M | ≈ ¥0.003 | 约 330 次 |
| deepseek-ai/DeepSeek-V3 | ¥2-3/M | ≈ ¥0.0016 | 约 600 次 |
| deepseek-ai/DeepSeek-R1 | ¥4-16/M | ≈ ¥0.005 | 约 200 次 |

> 💡 按 30 次/日 × 30 天 ≈ 900 次/月 计算，使用免费模型 **0 元**，使用 7B 模型每月花费**不到 1 元**。价格以 [硅基流动官网](https://cloud.siliconflow.cn/) 为准。

---

## 🖥️ 使用建议

1. **先小规模测试**：建议先设置「每日上限 = 5」跑一轮，确认发送正常后再调大
2. **间隔不宜过短**：过快投递可能触发平台风控，建议 ≥ 30 秒
3. **AI 生成前填写技能**：避免 AI 写出你不会的技能，造成面试翻车
4. **保持登录状态**：脚本运行期间请勿手动切换账号或退出登录
5. **运行中不要手动操作**：脚本会自动点击页面元素，运行期间请勿干预页面

---

## ❓ 常见问题（FAQ）

**Q1：面板没出现？**
检查是否在 `www.zhipin.com/web/geek/*` 页面（职位搜索页/聊天页）；刷新页面重试；确认 Tampermonkey 已启用脚本。

**Q2：点击开始后提示"请在职位列表页开始"？**
脚本只在职位搜索页运行，请打开如 `https://www.zhipin.com/web/geek/jobs?query=AI` 的页面。

**Q3：AI 生成失败 / 没反应？**
检查 AI 配置页是否已填写有效的 API Key；可先取消「AI」开关验证基础投递流程是否正常。

**Q4：发送失败怎么处理？**
脚本会自动重试 3 次；仍失败则记录到跳过名单（当天不再投该职位），并继续下一个职位，不会卡死。

**Q5：停止按钮点了没反应？**
最长等待 1~2 秒（当前步骤结束）即停止。若在聊天页点停止，会停留在聊天页不再跳转，手动返回列表页即可。

**Q6：更换浏览器/电脑后配置丢失？**
配置保存在 Tampermonkey 的 GM 存储中，与浏览器绑定。换设备需重新导入脚本并重新配置（可同步 Tampermonkey 账号）。

**Q7：脚本会不会泄露我的 API Key？**
API Key 仅保存在**本机浏览器**的 Tampermonkey 存储中，只在调用硅基流动 API 时发送，不会上传到任何第三方服务器。

---

## ⚠️ 免责声明

1. 本项目仅供**个人学习交流**使用，请遵守 BOSS直聘平台规则及当地法律法规
2. 高频自动投递可能违反平台用户协议，**由此产生的账号风险由使用者自行承担**
3. 请合理设置投递频率与每日上限，避免对招聘方造成骚扰
4. 使用者应对 AI 生成的招呼语内容负责，请确保内容真实、准确

---

## 📜 版本记录

| 版本 | 内容 |
|------|------|
| v4.6 | 模型列表改为从硅基流动 API 实时获取（不写死），支持过滤 |
| v4.5 | 新增「我的技能」配置，AI 生成硬约束（禁止编造技能） |
| v4.4 | 移除硬编码 API Key（完全前台配置）；模板改为前台可编辑 |
| v4.3 | 新增 AI 配置标签页（API Key / 模型 / 费用参考） |
| v4.2 | 参数可配置化：每日上限、间隔秒数；移除默认招呼语 |
| v4.1 | 修复停止按钮失效（停止后不再续跑） |
| v4.0 | 会话跨页面持久化，聊天页自动返回列表继续投递 |
| v3.0 | 改为 DOM 直读（不依赖 window._jobInfo） |

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request。若 BOSS直聘页面改版导致选择器失效，请附带页面截图或 DOM 结构说明。
